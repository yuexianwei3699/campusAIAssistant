# 基于大语言模型的校园智能问答与个性化服务系统 - 开发规范与流程文档

---

## 目录

1. 项目概述
2. 技术选型与版本约束
3. 系统总体架构
4. 项目结构规范
5. 后端架构设计规范
6. 前端架构设计规范
7. 数据库设计规范
8. 知识库与大模型调用流程
9. API 接口规范
10. 安全规范
11. 开发流程
12. 测试规范
13. 部署与运维规范
14. 附录

---

## 1. 项目概述

### 1.1 文档目的

本文档用于规范**基于大语言模型的校园智能问答与个性化服务系统**的开发过程，明确技术栈、系统架构、项目目录、编码规则、接口标准、测试流程和部署流程，保证项目开发过程统一、可维护、可扩展。

### 1.2 项目定位

系统面向学生、教师、管理员和运维人员，提供校园智能问答、个性化服务、知识库管理、问答记录管理、通知公告管理、模型配置和数据统计分析等功能。

核心目标如下：

| 目标 | 说明 |
| --- | --- |
| 智能问答 | 用户通过自然语言查询校园政策、办事流程、通知公告和常见问题 |
| 知识库增强 | 通过校园知识库和向量检索约束大模型回答，降低幻觉 |
| 个性化服务 | 根据角色、学院、专业、年级等信息推荐相关通知和待办 |
| 后台管理 | 管理员维护用户、知识、问答记录、公告和系统配置 |
| 数据分析 | 统计热门问题、满意度、无法回答问题和模型调用情况 |

---

## 2. 技术选型与版本约束

本项目采用固定技术栈，不再使用多方案并列选型。

| 层级 | 技术 | 版本/要求 | 用途 |
| --- | --- | --- | --- |
| 前端框架 | Vue 3 | 3.x | 用户端和管理后台 |
| 前端构建 | Vite | 5.x 或以上 | 前端构建与开发服务器 |
| UI 组件库 | Element Plus | 2.x | 后台表格、表单、弹窗、导航 |
| 状态管理 | Pinia | 2.x | 用户信息、权限、会话状态 |
| 路由 | Vue Router | 4.x | 页面路由与权限路由 |
| HTTP 客户端 | Axios | 1.x | 请求后端接口 |
| 后端框架 | Spring Boot | 3.x | REST API 与业务服务 |
| 权限认证 | Spring Security + JWT | 固定采用 | 登录认证与接口鉴权 |
| ORM | MyBatis-Plus | 3.x | 数据访问层开发 |
| 数据库 | MySQL Docker 镜像 | 8.x | 用户、知识、问答、日志等业务数据 |
| 缓存 | Redis Docker 镜像 | 7.x | 登录状态、限流、热门问答缓存 |
| 向量检索 | Milvus Docker 镜像 | 2.x | 知识片段向量存储与语义检索 |
| 文档解析 | Apache Tika | 2.x | PDF、Word、Excel、TXT 文档解析 |
| 大模型接入 | 第三方 LLM API | OpenAI 兼容接口优先 | 回答生成、问题理解 |
| 接口文档 | Knife4j / Swagger | Spring Boot 兼容版本 | API 调试与说明 |
| 构建工具 | Maven | 3.8+ | 后端依赖管理 |
| 容器编排 | Docker Compose | 2.x | 本地启动 MySQL、Redis、Milvus 等基础服务 |

---

## 3. 系统总体架构

### 3.1 总体架构图

```text
┌──────────────────────────────────────────────────────┐
│ 前端层 Vue 3 + Element Plus                           │
│ 学生端 / 教师端 / 管理后台 / 数据统计页面              │
└──────────────────────────┬───────────────────────────┘
                           │ HTTP / JSON / JWT
┌──────────────────────────▼───────────────────────────┐
│ 接口层 Spring Boot Controller                         │
│ Auth / User / Chat / Knowledge / Notice / Admin / Stats│
└──────────────────────────┬───────────────────────────┘
                           │
┌──────────────────────────▼───────────────────────────┐
│ 业务层 Service                                         │
│ 用户权限 / 智能问答 / 知识库 / 个性化推荐 / 日志统计    │
└──────────────────────────┬───────────────────────────┘
                           │
┌──────────────────────────▼───────────────────────────┐
│ 智能处理层                                             │
│ 文档解析 / 文本切分 / Embedding / Milvus 检索 / LLM调用 │
└──────────────────────────┬───────────────────────────┘
                           │
┌──────────────────────────▼───────────────────────────┐
│ 数据层                                                 │
│ MySQL / Redis / Milvus / 本地文件存储                  │
└──────────────────────────────────────────────────────┘
```

### 3.2 模块划分

| 模块 | 职责 |
| --- | --- |
| 用户认证模块 | 登录、注册、JWT 签发、密码加密、用户资料维护 |
| 权限管理模块 | 角色、菜单、接口权限控制 |
| 智能问答模块 | 会话管理、问题处理、知识检索、模型回答、来源展示 |
| 知识库模块 | 分类、标签、文件上传、文档解析、知识审核、上下架 |
| 个性化服务模块 | 按角色、学院、专业、年级推荐通知、待办和常用问答 |
| 通知公告模块 | 通知发布、查询、状态管理、适用人群配置 |
| 问答反馈模块 | 点赞、点踩、文字反馈、低满意度问题分析 |
| 数据统计模块 | 提问量、热门问题、满意度、无法回答问题、模型调用统计 |
| 系统配置模块 | 大模型 API、提示词模板、限流参数、系统开关配置 |
| 日志审计模块 | 登录日志、操作日志、异常日志、模型调用日志 |

---

## 4. 项目结构规范

### 4.1 根目录结构

```text
campus-ai-assistant/
├── backend/                 # Spring Boot 后端工程
├── frontend/                # Vue 3 前端工程
├── docker/                  # Docker 配置、数据库初始化脚本
├── docs/                    # 需求、设计、接口、部署等文档
├── scripts/                 # 初始化脚本、部署脚本
├── data/                    # 示例知识库数据，不存放隐私数据
├── docker-compose.yml       # 本地基础服务编排
├── README.md
└── 系统设计规范.md
```

### 4.2 后端目录结构

```text
backend/
├── pom.xml
├── src/main/java/com/campus/ai/
│   ├── CampusAiApplication.java
│   ├── common/              # 通用响应、异常、常量、工具类
│   ├── config/              # Spring Security、Redis、Swagger、Milvus配置
│   ├── controller/          # REST 接口控制器
│   ├── service/             # 业务接口
│   ├── service/impl/        # 业务实现
│   ├── mapper/              # MyBatis-Plus Mapper
│   ├── entity/              # 数据库实体
│   ├── dto/                 # 请求 DTO
│   ├── vo/                  # 响应 VO
│   ├── security/            # JWT、认证过滤器、权限处理
│   ├── llm/                 # 大模型客户端、提示词模板、模型日志
│   ├── rag/                 # 文本切分、向量化、检索、重排序
│   └── task/                # 定时任务
└── src/main/resources/
    ├── application.yml
    ├── application-dev.yml
    ├── mapper/
    └── db/migration/
```

### 4.3 前端目录结构

```text
frontend/
├── package.json
├── vite.config.ts
├── src/
│   ├── api/                 # Axios 接口封装
│   ├── assets/              # 图片、样式资源
│   ├── components/          # 通用组件
│   ├── layouts/             # 用户端布局、后台布局
│   ├── router/              # Vue Router 路由配置
│   ├── stores/              # Pinia 状态管理
│   ├── views/
│   │   ├── auth/            # 登录、注册
│   │   ├── chat/            # 智能问答
│   │   ├── profile/         # 个人中心
│   │   └── admin/           # 后台管理页面
│   └── utils/               # 工具函数、权限判断
└── public/
```

---

## 5. 后端架构设计规范

### 5.1 分层职责

| 层级 | 包名 | 职责 |
| --- | --- | --- |
| Controller | `controller` | 接收请求、参数校验、返回统一响应，不写业务逻辑 |
| Service | `service` | 定义业务接口 |
| ServiceImpl | `service.impl` | 实现核心业务流程和事务控制 |
| Mapper | `mapper` | 数据库访问，禁止写复杂业务判断 |
| Entity | `entity` | 与数据库表结构对应 |
| DTO | `dto` | 接收前端请求参数 |
| VO | `vo` | 返回前端展示数据 |
| Common | `common` | 通用异常、响应对象、枚举、工具类 |

### 5.2 统一响应格式

```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

后端统一使用 `Result<T>` 返回，禁止 Controller 直接返回未封装对象。

### 5.3 异常处理

后端必须使用全局异常处理器 `GlobalExceptionHandler`，常见错误码如下：

| code | 含义 |
| --- | --- |
| 0 | 成功 |
| 400 | 参数错误 |
| 401 | 未登录或 Token 失效 |
| 403 | 无权限 |
| 404 | 资源不存在 |
| 500 | 系统异常 |
| 600 | 大模型服务异常 |

### 5.4 事务规范

以下场景必须使用事务：

1. 用户注册并初始化角色信息。
2. 知识文件上传、解析、入库和向量化状态更新。
3. 删除或下架知识内容。
4. 问答消息、来源记录和模型调用日志写入。
5. 管理员批量操作用户、知识或通知。

---

## 6. 前端架构设计规范

### 6.1 页面设计

| 页面 | 路由 | 说明 |
| --- | --- | --- |
| 登录页 | `/login` | 用户登录，获取 JWT |
| 问答页 | `/chat` | 聊天式智能问答、来源展示、反馈 |
| 历史记录 | `/chat/history` | 查看个人历史会话 |
| 个人中心 | `/profile` | 用户资料、收藏、消息 |
| 管理首页 | `/admin/dashboard` | 统计概览 |
| 知识库管理 | `/admin/knowledge` | 分类、文件、知识条目、审核 |
| 用户管理 | `/admin/users` | 用户查询、编辑、禁用 |
| 通知公告 | `/admin/notices` | 公告发布和适用人群配置 |
| 问答记录 | `/admin/chat-records` | 问答检索、反馈查看 |
| 模型配置 | `/admin/model-config` | API 地址、密钥、提示词模板 |
| 系统日志 | `/admin/logs` | 登录、操作、异常、模型调用日志 |

### 6.2 前端开发规则

1. 页面组件使用 PascalCase 命名，例如 `ChatWindow.vue`。
2. API 文件按业务模块拆分，例如 `authApi.ts`、`knowledgeApi.ts`。
3. 路由权限通过 `meta.roles` 控制。
4. Axios 请求拦截器统一注入 JWT。
5. 响应拦截器统一处理 401、403、500 错误。
6. 后台表格必须支持分页、关键词搜索和状态筛选。
7. 删除、禁用、下架等操作必须二次确认。

---

## 7. 数据库设计规范

### 7.1 核心表设计

| 表名 | 说明 |
| --- | --- |
| `sys_user` | 用户信息 |
| `sys_role` | 角色信息 |
| `sys_user_role` | 用户角色关联 |
| `sys_menu` | 菜单与权限 |
| `knowledge_category` | 知识分类 |
| `knowledge_item` | 知识条目 |
| `knowledge_chunk` | 知识切片 |
| `knowledge_file` | 上传文件记录 |
| `chat_session` | 问答会话 |
| `chat_message` | 问答消息 |
| `chat_source` | 回答引用来源 |
| `chat_feedback` | 用户反馈 |
| `notice` | 通知公告 |
| `model_config` | 模型配置 |
| `model_call_log` | 模型调用日志 |
| `operation_log` | 后台操作日志 |

### 7.2 命名规范

1. 表名和字段名使用小写英文与下划线。
2. 主键统一为 `id`，类型使用 `BIGINT`。
3. 创建时间和更新时间统一为 `created_at`、`updated_at`。
4. 逻辑删除字段统一为 `deleted`。
5. 状态字段统一为 `status`，枚举值在后端统一维护。
6. 外键字段使用 `{关联对象}_id`，例如 `user_id`、`category_id`。

### 7.3 关键字段示例

`knowledge_item` 应包含：

| 字段 | 说明 |
| --- | --- |
| `id` | 主键 |
| `title` | 知识标题 |
| `content` | 正文 |
| `category_id` | 分类 ID |
| `source_name` | 来源名称 |
| `source_url` | 来源地址 |
| `applicable_roles` | 适用角色 |
| `tags` | 标签 |
| `status` | 草稿、待审核、已发布、已下架 |
| `created_at` | 创建时间 |
| `updated_at` | 更新时间 |

---

## 8. 知识库与大模型调用流程

### 8.1 知识入库流程

```text
管理员上传文件
  ↓
Apache Tika 解析文本
  ↓
文本清洗与切分
  ↓
生成知识条目和知识片段
  ↓
调用 Embedding 接口生成向量
  ↓
写入 Milvus
  ↓
管理员审核发布
```

### 8.2 智能问答流程

```text
用户提问
  ↓
校验登录状态和用户角色
  ↓
保存用户问题
  ↓
根据问题生成向量
  ↓
Milvus 检索相似知识片段
  ↓
按角色、状态、分类过滤知识
  ↓
构造提示词
  ↓
调用大模型 API
  ↓
保存回答、引用来源、模型日志
  ↓
返回回答内容、来源列表、推荐问题
```

### 8.3 提示词约束

大模型提示词必须包含以下约束：

1. 只能依据提供的校园知识回答。
2. 不确定时说明“暂未查询到明确依据”，并建议联系相关部门。
3. 不编造办理地点、材料、时间和政策依据。
4. 涉及成绩、处分、个人隐私等内容时提示通过官方系统查询。
5. 回答应简洁，优先使用步骤化表达。

---

## 9. API 接口规范

### 9.1 URL 规范

接口统一以 `/api` 开头，管理端接口统一以 `/api/admin` 开头。

| 功能 | 方法 | 路径 |
| --- | --- | --- |
| 登录 | POST | `/api/auth/login` |
| 当前用户 | GET | `/api/users/me` |
| 发送问题 | POST | `/api/chat/messages` |
| 会话列表 | GET | `/api/chat/sessions` |
| 提交反馈 | POST | `/api/chat/feedback` |
| 知识列表 | GET | `/api/admin/knowledge-items` |
| 上传知识文件 | POST | `/api/admin/knowledge-files` |
| 发布通知 | POST | `/api/admin/notices` |
| 问答统计 | GET | `/api/admin/statistics/questions` |

### 9.2 请求规范

1. `POST` 和 `PUT` 请求使用 JSON 请求体。
2. 分页参数统一为 `page`、`pageSize`。
3. 时间字段统一使用 `yyyy-MM-dd HH:mm:ss`。
4. 文件上传使用 `multipart/form-data`。
5. JWT 通过请求头 `Authorization: Bearer {token}` 传递。

---

## 10. 安全规范

1. 密码使用 BCrypt 加密存储。
2. 模型 API Key 只允许后端读取，不得返回前端。
3. 管理端接口必须校验管理员权限。
4. 上传文件必须限制格式、大小并进行安全校验。
5. 日志中不得记录明文密码、完整 Token、模型密钥。
6. 学生个人信息按最小必要原则存储。
7. 对高频问答请求进行 Redis 限流。
8. 对模型输出进行敏感内容过滤。

---

## 11. 开发流程

### 11.1 阶段划分

| 阶段 | 任务 | 输出物 |
| --- | --- | --- |
| 第 1 阶段 | 需求确认、技术栈确认、数据库初稿 | 需求文档、系统设计规范、ER 图 |
| 第 2 阶段 | 搭建 Spring Boot 和 Vue 基础工程 | 后端骨架、前端骨架、统一响应格式 |
| 第 3 阶段 | 实现登录、JWT、角色权限 | 用户认证模块、权限路由 |
| 第 4 阶段 | 实现知识库分类、条目、文件上传 | 知识库后台管理 |
| 第 5 阶段 | 实现文档解析、切分、向量化、Milvus 检索 | RAG 检索链路 |
| 第 6 阶段 | 实现智能问答、多轮会话、来源展示 | 用户端问答功能 |
| 第 7 阶段 | 实现通知公告、个性化推荐、反馈 | 个性化服务模块 |
| 第 8 阶段 | 实现统计分析、日志、模型配置 | 管理后台完善 |
| 第 9 阶段 | 系统测试、部署、论文材料整理 | 测试报告、部署说明、演示系统 |

### 11.2 开发顺序要求

开发必须先完成基础能力，再开发智能能力：

```text
项目骨架 → 数据库 → 登录权限 → 知识库管理 → 文件解析
→ 向量检索 → 大模型问答 → 个性化服务 → 统计分析 → 部署测试
```

### 11.3 GitHub 开发规范

本项目使用 Git + GitHub 进行版本管理。代码、文档和配置示例应提交到仓库，依赖目录、构建产物、密钥和真实数据不得提交。

首次接入 GitHub 时，如远程仓库已包含 `README.md`、`.gitignore` 或 `LICENSE` 等初始提交，本地推送前必须先拉取远程内容：

```bash
git pull origin main --allow-unrelated-histories
git push -u origin main
```

禁止直接使用 `git push --force` 覆盖远程仓库，除非明确确认远程提交可以丢弃。

分支命名如下：

| 分支 | 用途 |
| --- | --- |
| `main` | 稳定版本 |
| `dev` | 日常开发 |
| `feature/auth` | 登录权限功能 |
| `feature/knowledge` | 知识库功能 |
| `feature/chat` | 智能问答功能 |

日常开发流程如下：

```bash
git status
git add .
git commit -m "提交说明"
git push
```

提交信息使用动宾结构，说明本次提交完成的具体内容，例如：

- `完成用户登录接口`
- `新增知识库文件上传功能`
- `修复问答来源为空的问题`

出现合并冲突时，先查看冲突文件：

```bash
git status
```

手动处理冲突后再提交：

```bash
git add .
git commit -m "合并远程仓库变更"
git push
```

`.gitignore` 至少应包含：

```gitignore
node_modules/
dist/
target/
.env
.env.*
*.log
uploads/
```

---

## 12. 测试规范

### 12.1 后端测试

后端至少覆盖以下测试：

1. 登录成功、登录失败、Token 过期。
2. 学生访问管理接口应返回 403。
3. 管理员可新增、审核、下架知识。
4. 未发布知识不能被问答检索引用。
5. 大模型接口异常时返回兜底提示。
6. 问答反馈能够正确写入数据库。

### 12.2 前端测试

前端重点验证：

1. 登录后按角色加载菜单。
2. 问答页面能展示回答、来源和反馈按钮。
3. 管理后台表格分页、搜索、筛选正常。
4. 删除、下架、禁用操作有二次确认。
5. 401、403、500 错误能够统一提示。

### 12.3 集成测试流程

```text
通过 Docker Compose 启动 MySQL、Redis、Milvus
  ↓
启动 Spring Boot 后端
  ↓
启动 Vue 前端
  ↓
导入测试知识文件
  ↓
执行智能问答测试
  ↓
检查数据库、向量库、日志和前端展示
```

---

## 13. 部署与运维规范

### 13.1 Docker 基础服务部署

本项目后端依赖的 MySQL、Redis、Milvus 统一使用 Docker 部署，本机不直接安装数据库服务。Docker Compose 文件放在项目根目录 `docker-compose.yml`，数据库初始化 SQL 放在 `docker/mysql/init/`。

基础服务包括：

| 服务 | 镜像 | 端口 | 用途 |
| --- | --- | --- | --- |
| MySQL | `mysql:8.0` | `3306` | 业务数据存储 |
| Redis | `redis:7` | `6379` | 缓存、限流、会话辅助 |
| Milvus | `milvusdb/milvus` | `19530` | 向量数据存储与语义检索 |
| MinIO | `minio/minio` | `9000`、`9001` | Milvus 对象存储依赖 |
| Etcd | `quay.io/coreos/etcd` | `2379` | Milvus 元数据依赖 |

启动基础服务：

```bash
docker compose up -d
```

查看服务状态：

```bash
docker compose ps
```

停止基础服务：

```bash
docker compose down
```

如需清空本地数据库和向量库数据，必须先确认数据可丢弃，再删除 Docker volume。

### 13.2 本地应用启动

后端：

```bash
cd backend
mvn spring-boot:run
```

前端：

```bash
cd frontend
npm install
npm run dev
```

本地开发启动顺序固定为：

```text
docker compose up -d
  ↓
mvn spring-boot:run
  ↓
npm run dev
```

### 13.3 配置文件规范

后端配置按环境拆分：

```text
application.yml
application-dev.yml
application-prod.yml
```

敏感配置通过环境变量读取：

```env
DB_PASSWORD=your-password
REDIS_PASSWORD=your-password
MILVUS_HOST=localhost
MILVUS_PORT=19530
LLM_API_KEY=your-api-key
JWT_SECRET=your-jwt-secret
```

数据库连接配置应指向 Docker 暴露端口：

| 配置项 | 本地开发值 |
| --- | --- |
| `DB_HOST` | `localhost` |
| `DB_PORT` | `3306` |
| `REDIS_HOST` | `localhost` |
| `REDIS_PORT` | `6379` |
| `MILVUS_HOST` | `localhost` |
| `MILVUS_PORT` | `19530` |

### 13.4 运维要求

1. 定期备份 MySQL 数据库和上传文件。
2. 定期检查 Milvus 向量数据与知识库状态是否一致。
3. 定期检查模型调用失败率、响应时间和费用消耗。
4. 发现错误回答时，应根据问答记录定位知识来源并修正。
5. 发布新版本前必须完成登录、知识库、问答、后台管理核心流程回归测试。

---

## 14. 附录

### 14.1 术语

| 术语 | 说明 |
| --- | --- |
| LLM | 大语言模型，用于自然语言理解和回答生成 |
| RAG | 检索增强生成，通过知识库检索结果约束模型回答 |
| Embedding | 文本向量化表示 |
| Milvus | 向量数据库，用于语义检索 |
| JWT | 无状态登录令牌 |
| RBAC | 基于角色的访问控制 |

### 14.2 毕设优先实现范围

毕业设计阶段必须优先完成：

1. Vue 3 前端基础页面。
2. Spring Boot 后端基础接口。
3. 用户登录、角色权限和管理后台。
4. 知识库管理、文件上传和文档解析。
5. Milvus 语义检索和大模型问答。
6. 来源展示、问答历史和反馈。
7. 基础统计分析和系统日志。
